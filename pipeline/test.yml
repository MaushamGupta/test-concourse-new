platform: linux
inputs:
   - name: test-git-resource
image_resource:
    type: docker-image
    source: {repository: node, tag: "6" }
run:
    path: sh
    args: ["./test-git-resource/pipeline/install.sh"]
 #   - -exc
 #   - |
 #     cd test-git-resource/pipeline
 ##     test-git-resource/pipeline/install.sh
