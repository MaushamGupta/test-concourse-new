platform: linux
inputs:
   - name: test-git-resource
image_resource:
    type: docker-image
    source: {repository: node, tag: "6" }
run:
    path: sh
    args:
    - -exc
    - |
      cd test-git-resource/pipeline
      chmod +x install.sh
      install.sh
